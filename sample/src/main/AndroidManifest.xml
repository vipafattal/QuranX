<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.abedfattal.quranx.sample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".QuranXApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MushafLibrary"
        android:usesCleartextTraffic="true"
        tools:targetApi="m"
        tools:ignore="AllowBackup">
        <activity
            android:name=".MainActivity"
            android:label="@string/samples"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".wordsprocessor.WordsProcessorActivity"
            android:exported="true" />
        <activity
            android:name=".core.QuranManagementActivity"
            android:exported="true" />
        <activity
            android:name=".tajweedprocessor.TajweedProcessorActivity"
            android:exported="true" />
        <activity
            android:name=".tajweedrules.TajweedRulesActivity"
            android:exported="true" />
    </application>

</manifest>